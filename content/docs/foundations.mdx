---
title: Foundations
description: Shared decisions for recognisable, accessible TeacherActive products.
---

## Brand and colour

Use TA Blue `#005292` for structure and TA Orange `#F57D00` for emphasis. Components consume semantic tokens, not application-specific colour values. The primary action button follows the brand colour hierarchy (Design 2): TA Blue `--action` with a white `--action-foreground` label, which is 8.01:1 and meets WCAG AA. Orange appears as the `--action-accent` border or underline, never behind or as button text, because white on the brand orange is only 2.69:1.

## Typography and space

Use Arial with documented system fallbacks. Keep compact headings, readable body text and deliberate space between related elements. Do not copy presentation point sizes directly into product UI.

## Accessibility

Target WCAG 2.2 AA. Preserve keyboard operation, visible focus, text scaling, reduced motion and forced-colour support. Automated checks supplement keyboard and screen-reader review.

## Themes

Themes change presentation, not semantics or interaction contracts. Approved themes and a real token-driven Styling canvas will follow the shared component implementation.
