import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'TeacherActive design system' };
export default function HomePage() {
  return <main><h1>Shared foundations. Consistent experiences.</h1><p>Accessible components and page templates for building TeacherActive products.</p></main>;
}
